import { QuestionCommentModel, TextAreaModel, SurveyError } from "survey-core";
export declare class QuestionPresetJsonModel extends QuestionCommentModel {
    private jsonAreaModelValue;
    private jsonErrors;
    constructor(name: string);
    getType(): string;
    protected getCssType(): string;
    getTemplate(): string;
    get textAreaModel(): TextAreaModel;
    protected onCheckForErrors(errors: Array<SurveyError>, isOnValueChanged: boolean, fireCallback: boolean): void;
}
